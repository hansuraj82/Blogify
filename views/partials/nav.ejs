<nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm py-3">
  <div class="container">
    <a class="navbar-brand fw-bold text-primary" href="/">Blogify</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
      aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse justify-content-between" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link <% if (locals.current === 'home') { %>active fw-bold<% } %>" href="/">Home</a>
        </li>
        <% if (locals.user) { %>
          <li class="nav-item">
            <a class="nav-link <% if (locals.current === 'create') { %>active fw-bold<% } %>"
              href="/blog/add-blog">Create Blog</a>
          </li>
          <% } %>
      </ul>

      <ul class="navbar-nav">
        <% if (locals.user) { %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle d-flex align-items-center gap-2 text-dark" href="#" role="button"
              data-bs-toggle="dropdown" aria-expanded="false">
              <img src="<%= locals.user.profile %>" alt="Profile" class="rounded-circle" width="32" height="32"
                style="object-fit: cover; border: 2px solid #0d6efd;">
              <span class="fw-semibold">
                <%= locals.user.fullName || 'User' %>
              </span>
            </a>

            <ul class="dropdown-menu dropdown-menu-end border-0 shadow-lg p-0"
              style="min-width: 280px; border-radius: 12px;">
              <li class="p-3">
                <div class="text-center">
                  <img src="<%= locals.user.profile %>"
                    class="rounded-circle mb-3 border border-2 border-primary shadow-sm" width="80" height="80"
                    style="object-fit: cover;" alt="Profile">
                  <h6 class="fw-bold mb-0">
                    <%= locals.user.fullName %>
                  </h6>
                  <p class="text-muted mb-1 small">
                    <%= locals.user.email %>
                  </p>
                  <span class="badge bg-success text border border-1 text-uppercase px-2">
                    <%= locals.user.role || 'User' %>
                  </span>
                </div>
              </li>
              <li>
                <hr class="dropdown-divider m-0">
              </li>
              <li>
                <a class="dropdown-item text-center fw-semibold text-danger py-2" href="/user/logout">
                  <i class="bi bi-box-arrow-right me-1"></i> Logout
                </a>
              </li>
            </ul>
          </li>

          <% } else { %>
            <li class="nav-item">
              <a class="nav-link text-success fw-semibold" href="/user/signin">Sign In</a>
            </li>
            <li class="nav-item">
              <a class="nav-link text-primary fw-semibold" href="/user/signup">Sign Up</a>
            </li>
            <% } %>
      </ul>
    </div>
  </div>
</nav>

<% if (locals.error) { %>
  <div class="container mt-4">
    <div class="alert alert-danger shadow-sm" role="alert">
      <%= locals.error %>
    </div>
  </div>
  <% } %>